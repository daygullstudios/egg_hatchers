{
  "name": "nestarium-multiplayer-playtest",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && node --test tool/operator_node_tests.mjs",
    "migration:operator": "node tool/migration_operator.mjs",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "canary:dry-run": "wrangler deploy --config wrangler.canary.jsonc --dry-run",
    "canary:deploy": "wrangler deploy --config wrangler.canary.jsonc"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.4",
    "@vitest/runner": "4.1.11",
    "@vitest/snapshot": "4.1.11",
    "typescript": "5.9.2",
    "vitest": "4.1.11",
    "wrangler": "4.129.0"
  }
}
